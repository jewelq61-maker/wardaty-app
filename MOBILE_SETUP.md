# 📱 دليل إعداد الأيقونات وشاشات البداية للجوال

## ✅ ما تم إنجازه

تم إنشاء:
- ✅ أيقونة التطبيق الرئيسية (1024x1024) في `public/icon.png`
- ✅ شاشة البداية (1080x1920) في `public/splash.png`
- ✅ ملف Manifest للـ PWA في `public/manifest.json`

## 🎨 الأيقونات المُنشأة

### أيقونة التطبيق
- **الملف**: `public/icon.png`
- **الحجم**: 1024x1024 بكسل
- **التصميم**: زهرة لوتس وردية أنيقة بتدرجات magenta
- **الاستخدام**: أيقونة رئيسية لـ iOS App Store و Google Play Store

### شاشة البداية
- **الملف**: `public/splash.png`
- **الحجم**: 1080x1920 بكسل
- **التصميم**: خلفية متدرجة وردية مع وردة وشعار التطبيق
- **الاستخدام**: شاشة البداية للتطبيق

## 📦 الخطوات التالية (بعد نقل المشروع إلى GitHub)

### 1. تثبيت أدوات إنشاء الأيقونات

```bash
npm install -g cordova-res
```

### 2. إنشاء مجلد الـ resources

```bash
mkdir -p resources
```

### 3. نسخ الأيقونة وشاشة البداية

```bash
cp public/icon.png resources/icon.png
cp public/splash.png resources/splash.png
```

### 4. إنشاء جميع الأحجام المطلوبة

```bash
cordova-res ios --skip-config --copy
cordova-res android --skip-config --copy
```

هذا الأمر سيُنشئ تلقائياً:

#### لـ iOS:
- أيقونات بأحجام: 20x20, 29x29, 40x40, 50x50, 57x57, 58x58, 60x60, 72x72, 76x76, 80x80, 87x87, 100x100, 114x114, 120x120, 144x144, 152x152, 167x167, 180x180, 1024x1024
- شاشات بداية بأحجام مختلفة للـ iPhone و iPad

#### لـ Android:
- أيقونات بأحجام: ldpi (36x36), mdpi (48x48), hdpi (72x72), xhdpi (96x96), xxhdpi (144x144), xxxhdpi (192x192)
- شاشات بداية بأحجام مختلفة

### 5. الموقع النهائي للملفات

بعد تشغيل الأمر أعلاه، ستجد الملفات في:

```
ios/App/App/Assets.xcassets/AppIcon.appiconset/
ios/App/App/Assets.xcassets/Splash.imageset/
android/app/src/main/res/mipmap-*/
android/app/src/main/res/drawable-*/
```

## 🎯 إعدادات Capacitor

تم تكوين `capacitor.config.ts` بالفعل مع:
- App ID: `app.lovable.38193668a92f4b81ad37f226055ff0a0`
- App Name: `Wardaty | وردتي`
- الألوان والإعدادات المناسبة

## 🔄 تحديث الأيقونات لاحقاً

إذا أردت تغيير التصميم:
1. استبدل `resources/icon.png` و `resources/splash.png`
2. شغّل `cordova-res ios --skip-config --copy` و `cordova-res android --skip-config --copy`
3. شغّل `npx cap sync`

## 📱 اختبار التطبيق

بعد إضافة المنصات وإنشاء الأيقونات:

```bash
# لـ iOS
npx cap open ios
# ثم شغّل من Xcode

# لـ Android
npx cap open android
# ثم شغّل من Android Studio
```

## 🎨 معلومات التصميم

- **اللون الأساسي**: #EC4899 (Pink/Magenta)
- **اللون الثانوي**: #FDF2F8 (Light Pink)
- **النمط**: أنيق، أنثوي، بسيط
- **الرمز**: زهرة/وردة متفتحة

## ⚠️ ملاحظات هامة

1. **iOS**: تحتاج Mac مع Xcode لبناء التطبيق
2. **Android**: يمكن البناء على أي نظام تشغيل مع Android Studio
3. **الأحجام**: تأكد من أن الأيقونة الأصلية 1024x1024 بكسل على الأقل لأفضل جودة
4. **الخلفية**: استخدم خلفية شفافة أو صلبة حسب الحاجة
5. **النشر**: ستحتاج حساب Apple Developer (99$/سنة) وGoogle Play Developer (25$ مرة واحدة)

## 📚 موارد إضافية

- [دليل Capacitor الرسمي](https://capacitorjs.com/docs)
- [دليل cordova-res](https://github.com/ionic-team/cordova-res)
- [متطلبات أيقونات iOS](https://developer.apple.com/design/human-interface-guidelines/app-icons)
- [متطلبات أيقونات Android](https://developer.android.com/guide/practices/ui_guidelines/icon_design_launcher)
